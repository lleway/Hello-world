Change-cases
===============
1.Maybe later we can upload more format files instead of just one, 
  and we can distinguish different formats of files to judge.
2.We will add the download function later, 
  and we can download it after the difference 
  gene table and scatter plot have been returned. 
   
  (To Be Added...)